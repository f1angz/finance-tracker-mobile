<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Тема приложения Finance Tracker -->
    <style name="Theme.Financetrakcermobile" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/text_secondary</item>
        <item name="colorSecondaryVariant">@color/text_primary</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color (прозрачный для Compose). -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>